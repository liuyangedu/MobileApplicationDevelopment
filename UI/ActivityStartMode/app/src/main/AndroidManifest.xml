<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.edu.bupt.sdmda.activitystartmode">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StandardAct">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SingleTopAct" android:launchMode="singleTop"/>
        <activity android:name=".SingleTaskAct" android:launchMode="singleTask"/>
        <activity android:name=".SingleTaskAffAct"
            android:launchMode="singleTask"
            android:taskAffinity=".test"/>
        <activity android:name=".SingleInstance"
            android:launchMode="singleInstance"/>
    </application>

</manifest>